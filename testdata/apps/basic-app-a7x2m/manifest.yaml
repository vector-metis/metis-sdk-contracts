schema_version: 1
id: basic-app-a7x2m
version: 1.0.0
display_name: Basic Fixture
type: web
arch: [amd64]
dependencies: []
services:
  web:
    lifecycle: {restart: unless-stopped, required: true}
    endpoints:
      - {name: web, protocol: http, container_port: 8080}
